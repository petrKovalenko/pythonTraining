#matrix test
class Matrix:
    rows
    nOfRows
    nOfCols
    def _init_(self, rowsArray):
        if rowsArray is None    
def print_matrix(matrix):
    if matrix is None:
        print "Error - matrix is empty!"
        return -1
    
